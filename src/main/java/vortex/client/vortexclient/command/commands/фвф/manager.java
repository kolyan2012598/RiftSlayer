package vortex.client.vortexclient.command.commands.фвф;

import com.llamalad7.mixinextras.sugar.impl.handlers.HandlerTransformer;
import vortex.client.vortexclient.command.commands.SayCommand;
import vortex.client.vortexclient.module.Module;

public class manager
{
    public manager() {
        clone(new manager());
    }

    private void clone(manager manager) {
        Object HandlerTransformer = null;
        final Object handlerTransformer;
    }
}
